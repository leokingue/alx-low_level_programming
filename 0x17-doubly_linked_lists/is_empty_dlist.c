#include "lists.h"
/**
 * is_empty_dlist - check if a list is empty
 * @li: a doubly linked list
 * Return: true or false if it's isn't empty
 */

Bool is_empty_dlist(dlist li)
{
	if (li == NULL)
		return (true)
	return (false)
}
