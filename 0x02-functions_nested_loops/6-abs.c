#include<stdlib.h>
#include"main.h"
/**
 *_abs- printing absolute value of any number
 *
 * @n:parameter for the function
 * Return:0 if succcess
 */
int _abs(int n)
{
	int m;

	m = abs(n);
	return (m);
}
