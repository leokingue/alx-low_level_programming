#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a: first int argument
 * @b: second int argument
 * Return: result if succes 0 failed
 */
int mul(int a, int b)
{
	int resultat = a * b;

	return (resultat);
}
