#include "main.h"
#include <stdio.h>
/**
 * main - main function test
 * Return: 0
 */
int main(void)
{
	char word[] = "school";

	printf("the length is: %d for %s" my_strlen(word), word);
	return (0);
}
