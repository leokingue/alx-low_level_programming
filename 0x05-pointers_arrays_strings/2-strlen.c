#include "main.h"
/**
 * _strlen - a function that returns
 * the length of a string.
 * @s: char pointer argument
 * Return: a number
 */
int _strlen(char *s)
{
	int value = sizeof(s) / sizeof(char);

	return (value);
}
